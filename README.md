# Password-_Protection
A secure desktop application built in Java that allows users to register, log in, and manage personal notes with password protection. Features encrypted authentication using BCrypt, a SQLite database for persistent storage, and a clean Swing-based UI.
